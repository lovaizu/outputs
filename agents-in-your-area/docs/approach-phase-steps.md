# Approach Phase — Step List v8

Produced by integrating two independent expert reviews (product discovery + AI agent design)
and a subsequent re-review pass, then refined against Action Principles assay findings.

---

## Conventions

**Step types**

| Code | Meaning |
|------|---------|
| [I] | AI-internal step — no user interaction; runs to completion and writes CCS |
| [E] | Expert gate — requires `/ty` or `/gm` before any next step executes |
| [R] | Research step — reads sources (codebase or ecosystem) and writes findings to CCS; no state changes to approach artifacts |
| [conditional] | Executes only when a specified trigger condition is met; skip path is explicitly defined |

**Commands:** `/ty` — approve and proceed. `/gm` — provide feedback or redirect; opens a revision loop or activates A-REDIRECT.

**CCS** (Compressed Cognitive State) — the bounded state passed between Turns. Nine components: `episodic_trace` (what just happened), `semantic_gist` (current task framing), `focal_entities` (entities being worked on), `relational_map` (how entities relate), `goal_orientation` (the end goal — Benefit + AS verbatim from goal.md, unchanged throughout Approach), `constraints` (what must not be done), `predictive_cue` (what to do next), `uncertainty_signal` (what is still uncertain), `retrieved_artifacts` (evidence with source citations). These definitions make this artifact self-contained; [acc.md](acc.md) documents implementation details (component encoding, session initialization). **AI-internal use:** CCS In/Out columns document AI state management only; experts see only the content described in each [E] step's Presentation structure notes. CCS component names are not exposed in expert-facing output.

**Phase completion state:** Approach phase is complete when: (1) `approach.md` is committed to the branch — verified by A18a: file exists at expected path with all required sections; (2) Testing, Technology, and Design are all locked — verified by CCS lock flags (Testing: A6b /ty; Technology: A12; Design: A16); (3) AS-coverage matrix has no orphaned AS rows — verified by A17 consistency report showing zero broken links; (4) all NOFIT compromise rows have been explicitly acknowledged by the expert — verified by A19 CCS In showing each NOFIT row acknowledged; (5) A19 `/ty` has been issued — verified by A19 CCS Out episodic_trace recording the /ty decision.

**Locked element summary:**

| Element | Locked at | What locking means |
|---------|-----------|-------------------|
| Testing | A6 (confirmed A6b) | Test runner, patterns, and AS→method mapping committed; all downstream steps treat Testing as fixed |
| Technology | A12 | Technology choice committed; must/must_not constraints derived; Design may begin |
| Design | A16 | Design decisions committed; A17 consistency validation may proceed |

---

## Step List

| ID | Name | Type | Purpose | CCS In (key components) | CCS Out (key components, success path) | Notes |
|----|------|------|---------|------------------------|---------------------------------------|-------|
| A0 | Approach Intake | [I] | Initialize CCS from goal.md; validate that all required Goal phase sections are present; seed initial relational_map from TC links. | ∅ (goal.md read directly) | episodic_trace (goal.md read summary, section validation result), semantic_gist (approach: investigate Testing/Technology/Design for the approved goal), focal_entities (AS identifiers, user segment, key domain concepts), relational_map (AS→Pain, Pain→Benefit — TC links inherited), goal_orientation (Benefit + AS verbatim from goal.md — unchanged throughout Approach), constraints (out-of-scope items from goal.md AS section if any), uncertainty_signal (missing goal.md sections if any) | **Required goal.md sections:** Situation, Pain, Benefit, Acceptance Scenarios (AS list), and out-of-scope items (if any). All five must be present; any missing section is set as uncertainty_signal (gap: goal.md [section] missing) and A0 halts. goal_orientation is initialized from Benefit + AS verbatim and persists unchanged for all Approach Turns. relational_map is seeded with TC links (AS→Pain, Pain→Benefit) so downstream steps extend it. |
| A1 | Investigation Plan | [I] | Generate a concrete investigation plan including a first-pass AS-to-test-method sketch so that the Planning Gate (A2) is a substantive review rather than a blank-plan rubber stamp. | semantic_gist (approach), focal_entities (AS identifiers), relational_map (TC links), goal_orientation, uncertainty_signal | semantic_gist (investigation plan: first-pass AS→test-method sketch, A5 audit targets by path/area, A7 technology categories, design unknowns), goal_orientation, predictive_cue (Planning Gate ready), uncertainty_signal (highest-uncertainty areas flagged) | **All names are provisional hypotheses:** A1 runs before any codebase reading (A5) or ecosystem research (A7). File/directory names and technology categories are unverified guesses derived from the AS and domain context — label them explicitly as "provisional" in the plan. A2 treats these as candidate scope, not verified facts. A5 confirms or revises file/directory names; A7 confirms or revises technology categories. A per-AS test-method sketch must be produced even if provisional, so that A2 is a judgment gate. |
| A2 | Planning Gate | [E] | Present investigation plan including first-pass AS→test-method sketch; obtain expert approval before investigation begins. | semantic_gist (investigation plan with first-pass sketch), focal_entities, relational_map, goal_orientation, uncertainty_signal, predictive_cue | episodic_trace (expert decision), semantic_gist (approved plan or revised plan from /gm), goal_orientation, predictive_cue (PROCEED → A3; plan revision → re-enter A1 via A-REDIRECT) | **Presentation structure:** Lead with Issue ("Is the investigation plan sufficient to produce an approach.md based on verified codebase facts and ecosystem research — not on assumptions? A grounded approach.md is one where every design and technology decision traces to a confirmed observation or cited external source.") and Conclusion (PROCEED or REVISE). Because PROCEED/REVISE is non-self-evident, add Rationale (one sentence of judgment reasoning — why the plan is or is not sufficient for grounded approach.md production — no plan details here) and then the full investigation plan (AS→test-method sketch, provisional file/directory list, technology categories) as Supporting material after the Conclusion. **Minimum adequacy criteria for /ty:** (1) every AS has a first-pass test method sketch, even if provisional; (2) at least one provisional file/directory is named per AS infrastructure question; (3) at least one technology category is named per AS domain; (4) no AS is left without a planned investigation target. **Minor vs. substantial revision:** A change to a technology category or file path is minor (absorbed inline). A change to a test method or the addition/removal of an AS scope item is substantial (re-enter A1 via A-REDIRECT). **Provisional names not approved by /ty:** Expert /ty at A2 approves the investigation scope and test-method sketch — not the provisional names. The gate package must explicitly state: "The following file/directory names and technology categories are unverified hypotheses; they must be confirmed or rejected at A5/A7 regardless of their inclusion in this plan. A5 must not treat their presence in this plan as evidence of their existence." |
| A3 | Testing Scoping | [I] | Map each AS to one or more test methods (unit, integration, E2E, manual); assess whether each AS is testable as written; identify AS-specific infrastructure questions for A5. | focal_entities (AS identifiers), goal_orientation, relational_map (AS→Pain), semantic_gist (approved plan with sketch), uncertainty_signal | semantic_gist (AS-to-test-method mapping with per-AS testability assessment, infrastructure questions derived from each AS), focal_entities (updated: test method as AS attribute), relational_map (AS→test-method edges added), goal_orientation, predictive_cue (Testing scope ready for review), uncertainty_signal (untestable AS flagged per AS with reason) | **Testability check per AS:** (1) observable and falsifiable as written, (2) mappable to a known test method. AS failing (1) is flagged in uncertainty_signal (gap: AS-N untestable — [reason]) and presented at A4 for cross-phase route. AS passing (1) but with no clear test method is also flagged (gap: AS-N — no method identified). **Verification method for the mapping:** The AS→method mapping is provisional at A3. It is confirmed when: (a) the method is executable with the actual test runner (verified at A6), (b) the AS is observable and falsifiable under that method (confirmed at A4), and (c) the design produces a test artifact covering the AS (verified at A17). A3's output is a hypothesis; the three-step confirmation is the verification. **Completeness check:** every AS in goal.md must appear in the mapping — none may be omitted. A4 confirms completeness before A5 begins. |
| A4 | Testing Scope Review | [E] | Present AS-to-test-method mapping and testability assessment; obtain expert confirmation; handle untestable AS via cross-phase route. | semantic_gist (AS-to-test-method mapping, testability assessment), focal_entities (AS with method attributes), relational_map, goal_orientation, predictive_cue, uncertainty_signal | episodic_trace (expert decision), semantic_gist (confirmed mapping or /gm instruction), goal_orientation, focal_entities, relational_map, uncertainty_signal, predictive_cue (PASS → A4b or A5; cross-phase → G21 if expert confirms untestable AS) | **Completeness confirmation:** Before /ty, expert confirms (1) every AS in goal.md appears in the mapping, (2) every test method has at least one A3 infrastructure question. If either condition fails, /gm re-enters A3. **Cross-phase route:** If expert confirms an AS is not testable as written, /gm routes to Goal phase G21 (AS Revision). Approach phase is suspended; it restarts from A0 after a new /go with the revised goal.md. /ty with partial flags opens A4b for the flagged items only. **G21 returns without AS change (deadlock):** If G21 reaffirms the AS as written without change, A4 does not re-enter as that would reproduce the same untestability flag. Instead, present the deadlock to the expert with options that avoid abandoning the goal before abort is offered: "AS-N was confirmed untestable in Approach and reaffirmed unchanged by Goal phase. Options: (1) accept AS-N as untestable with explicit acknowledgment (proceed with open risk documented in AS-coverage matrix); (2) remove AS-N from scope (revise goal.md via G21 with explicit removal). If options (1) and (2) have both been explicitly considered and rejected: (3) abort Approach — Approach is suspended; goal.md requires revision before a new /go." Expert must choose one before Approach proceeds. |
| A4b | Testing Scope Revision ×N | [E] | Revise the AS-to-test-method mapping per /gm feedback until the expert confirms or iteration limit is reached. | episodic_trace (prior exchange), semantic_gist (current mapping), focal_entities, relational_map, goal_orientation, predictive_cue (termination condition), uncertainty_signal | episodic_trace (updated), semantic_gist (revised mapping), focal_entities, relational_map, goal_orientation, uncertainty_signal | **Circuit-breaker:** Max N=5. On N=5 without /ty, emit an escalation Turn: (a) enumerate each specific unresolved finding from all N revision cycles individually (not subsumed into a summary); (b) present explicit options: (1) accept current mapping with open uncertainty_signals (acceptance rationale must state why each open finding does not block the goal); (2) revisit AS → cross-phase G21; (3) abort Approach — Approach is suspended; goal.md requires revision before a new /go. Expert must choose one. |
| A5 | Codebase Investigation | [R] | Read the repository to produce verified facts needed for Testing and Technology. Scope is set by the AS-specific infrastructure questions from A3 plus general architecture for Technology; it is not a generic checklist. | semantic_gist (confirmed AS-to-test mapping from A4/A4b, investigation plan from A1), focal_entities (AS identifiers, domain concepts), relational_map, goal_orientation, uncertainty_signal | retrieved_artifacts (codebase findings, each tagged with source path and the A3 question it answers: test runner identity, test file conventions, architecture notes, DB schema, dependency list, infrastructure constraints), uncertainty_signal (areas not readable or not answering A3 questions flagged) | **Provisional path verification:** For each provisional file/directory path from the A1 investigation plan, the first observation is whether that path exists in the repository. If a provisional path does not exist, set uncertainty_signal (gap: provisional path [P] not found — A3 question [Q] has no direct codebase anchor) rather than inferring answers from adjacent or similar files. **Pre-investigation scope:** Before beginning any file reads, state the complete investigation population: every directory and file type that could contain runner configuration, dependency declarations, or architecture constraints relevant to A3 questions. This pre-investigation scope definition is the reference against which the post-investigation scope statement is compared. Any area in the pre-investigation scope that was not read must be reported as uninspected. **Research hypothesis per A3 question:** Before reading, state the hypothesis for each A3 infrastructure question: e.g., "Test runner: hypothesis — project uses Jest, confirmed by presence of `jest.config.*` or `"jest"` in `package.json` devDependencies." Verification criterion must be a direct observation (file exists, key present, value matches), not an inference. **Scope statement:** After answering A3 questions, state the assumed codebase population inspected (which directories were read, which were not). For any uninspected area that could contain test configuration, dependency declarations, or architecture constraints relevant to A3 questions, set uncertainty_signal (gap: [area] uninspected — may affect [A3 question ID]). These uncertainty_signals are not optional flags — A6b must explicitly decide whether to block Testing lock or accept the gap before /ty is issued; A6 may not finalize strategy over an acknowledged uninspected area without A6b expert decision. **Scope is Testing-derived:** A3 infrastructure questions are the primary agenda; general architecture is secondary unless a structural constraint is found that would invalidate A3 test-method assumptions — in that case, set uncertainty_signal (gap: architecture constraint may affect test method for AS-N). **Failure mode:** Set uncertainty_signal (gap: [area] — could not read); A6 reads this and decides whether to block. |
| A6 | Testing Strategy Finalization | [I] | Finalize the Testing strategy using A5 findings: confirm the test runner, specify test patterns and file conventions, resolve AS infrastructure questions; produce the strategy for expert confirmation in A6b. | semantic_gist (confirmed AS-to-test mapping from A4/A4b, A5 investigation summary), retrieved_artifacts (A5), focal_entities (AS with method attributes), relational_map (AS→test-method), goal_orientation, uncertainty_signal | semantic_gist (finalized Testing strategy: runner, test patterns, AS coverage table with runner-confirmed methods), relational_map (AS→test-method edges finalized), focal_entities (updated with confirmed test method per AS), goal_orientation, uncertainty_signal (residual: AS whose test method depends on a Technology decision not yet made — flagged as pending, revisited at A12) | This step produces the strategy for A6b expert confirmation — it does not lock yet. **Runner confirmed** means: a configuration file specifying the runner was located AND its relevant content was read at A5, with the specific file path, key name, and value cited in retrieved_artifacts (e.g., `package.json` → `devDependencies.jest: "^29.0.0"`); locating the file without citing its content is not sufficient. Alternatively, the runner is declared explicitly in the package manager manifest with the specific manifest entry cited. The examples (`jest.config.*`, `vitest.config.*`, `pytest.ini`) are illustrative, not exhaustive; for any unfamiliar runner, check the runner's documentation to identify its configuration file before emitting uncertainty_signal. Inference from test file naming alone is not sufficient; set uncertainty_signal (gap: runner unconfirmed — [reason]) if no configuration was found and route to A-REDIRECT (re-enter A5 with narrowed scope). **Pending AS deferral rationale:** AS whose test method cannot be confirmed without knowing the Technology choice are flagged as pending and deferred to A12. Deferral is goal-serving because confirming them prematurely would produce a stale constraint if the Technology selection changes the runner compatibility. These AS remain open findings; none may still be pending at A17. |
| A6b | Testing Strategy Confirmation | [E] | Present the finalized Testing strategy (runner, patterns, AS coverage table) to the expert; confirm that A5 codebase findings are compatible with the A4-approved AS→method mapping. /ty locks the Testing element. | semantic_gist (finalized Testing strategy), focal_entities (AS with confirmed methods), relational_map, goal_orientation, uncertainty_signal | episodic_trace (expert decision), semantic_gist (confirmed strategy or /gm instruction), goal_orientation, focal_entities, relational_map, predictive_cue (PASS → Testing locked, proceed to A7; /gm → A6c) | **Mandate constraint:** A6b is strictly scoped to confirming that A5 codebase findings are compatible with the A4-approved AS→method mapping. A6b is not a second scope review. Changes to test methods or AS require /gm → A-REDIRECT re-entry ≤ A3, not inline revision at A6b. /ty locks the Testing element; all subsequent steps treat the Testing strategy as committed. |
| A6c | Testing Strategy Revision ×N | [E] | Revise the finalized Testing strategy per /gm feedback (e.g., runner choice, test pattern) until the expert confirms or iteration limit is reached. | episodic_trace (prior exchange), semantic_gist (current finalized strategy), focal_entities, relational_map, goal_orientation, predictive_cue (termination condition), uncertainty_signal | episodic_trace (updated), semantic_gist (revised strategy), focal_entities, relational_map, goal_orientation, uncertainty_signal | **Circuit-breaker:** Max N=5. On N=5 without /ty: enumerate each unresolved finding individually; if sticking point is runner/patterns → escalate (accept current with goal-tied rationale per finding / re-investigate A5); if sticking point is test methods or AS coverage → A-REDIRECT re-entry ≤ A3 or cross-phase to G21. |
| A7 | Ecosystem Technology Research | [R] | Research technology options relevant to the problem domain outside the repository: documentation, benchmarks, known limitations, adoption indicators. Runs after Testing is locked so research is constrained by confirmed test-method requirements. | semantic_gist (locked Testing strategy, investigation plan from A1), focal_entities (problem domain, confirmed test method requirements), goal_orientation, uncertainty_signal | retrieved_artifacts (ecosystem findings: candidate technologies, each with capability summary, testing-strategy compatibility, known limitations, adoption indicators — tagged with external source), uncertainty_signal (technology areas with no reliable data flagged) | **Pre-research population:** Before beginning research on any technology category, state the complete population of source types to be searched (e.g., official documentation, npm registry, GitHub repository, benchmark suites, developer surveys). This is the reference against which post-research source coverage is evaluated. Any source type not searched must be reported as unsearched with rationale. **Research hypothesis per technology category:** Before researching each category, state: "Technology T is a viable option if it satisfies [specific constraint from locked Testing strategy] and [stack constraint from A5]." **Verification method:** Confirmed when official documentation or a reproducible benchmark explicitly supports the constraint claim; source URL and retrieval date must be cited. Falsified when a documented limitation rules out the constraint; source cited. A technology with neither confirmed nor falsified hypothesis is set as uncertainty_signal (gap: [T] — insufficient data). **Adoption indicators** must cite a verifiable source (e.g., npm weekly downloads from npmjs.com, GitHub star count from the repository page, official survey). Uncited indicators are not accepted. Scope constraint: research is scoped to options compatible with the locked test runner. Options incompatible with the locked Testing strategy are recorded as ruled out with reason and cited source. **Failure mode:** Set uncertainty_signal (gap: [area] — no reliable data); A8 reads this. |
| A8 | Technology Options Enumeration | [I] | Enumerate candidate technology options filtered by: (a) locked Testing strategy compatibility, (b) stack constraints from A5, (c) ecosystem options from A7; for each, state capability match, stack compatibility, scope impact. | semantic_gist (locked Testing strategy, A5 codebase summary, A7 ecosystem summary), retrieved_artifacts (A5 + A7), focal_entities, relational_map, goal_orientation, uncertainty_signal | semantic_gist (technology candidate list: each option with capability match, stack compatibility, scope estimate, testing-strategy compatibility column), relational_map (candidate→constraint edges added), focal_entities (candidate entities added), goal_orientation, uncertainty_signal (candidates with unresolved uncertainty flagged) | Testing-strategy compatibility is a required column — options incompatible with the locked test methods are eliminated and recorded as ruled out with reason. |
| A9 | Technology Deep-Dive | [conditional] | Investigate shortlisted technology options where uncertainty remains after A8; if A8 flagged no uncertainty, skip this Turn (A10 reads A8's CCS directly) and record that deep-dive was not required. | semantic_gist (technology candidate list, flagged uncertainties from A8), retrieved_artifacts (A5 + A7), focal_entities, relational_map, goal_orientation, uncertainty_signal | **Active path:** retrieved_artifacts (deep-dive findings: per-option, tagged with option name and uncertainty resolved or confirmed unresolvable), uncertainty_signal (updated per option). **Skip path:** uncertainty_signal (level: low, note: deep-dive not required — no flagged candidates), CCS otherwise carried through unchanged. | A9 is [conditional]: triggered only when A8 uncertainty_signal flags at least one candidate. On the skip path, A10 reads A8's CCS unchanged — there is no phantom Turn between A8 and A10. |
| A10 | Technology Selection | [I] | Select one technology from the candidate list based on all verified findings; state explicit rationale tracing to A5/A7/A9 evidence; enumerate ruled-out options with reasons; if no viable option exists, emit no-fit state → A-NOFIT. | semantic_gist (technology candidate list), retrieved_artifacts (A5 + A7 + A9 if active), focal_entities, relational_map (candidate→constraint), goal_orientation, uncertainty_signal | **Selection path:** semantic_gist (selected technology + rationale, ruled-out list), relational_map (selected-technology→constraint edges added), focal_entities (selected technology entity), goal_orientation, uncertainty_signal (residual), predictive_cue (Technology Review ready). **No-fit path:** uncertainty_signal (level: high, gap: no option satisfies constraints — [per-option reason]), predictive_cue (route to A-NOFIT). | Selection must be a single choice. Conditional selections ("either X or Y") are not accepted. Rationale must trace to verified facts (A5/A7/A9), not to assumptions. If no option satisfies all constraints, emit no-fit state — do not force a selection. |
| A-NOFIT | No-Viable-Technology | [conditional] | Escalate a technology deadlock to the expert; present options for resolution; record the outcome in the AS-coverage matrix as a NOFIT compromise entry. | uncertainty_signal (no-fit gap per option), semantic_gist (candidate list and ruling reasons), constraints, goal_orientation | episodic_trace (expert decision), semantic_gist (chosen resolution: relax [constraint] / narrow [AS] / accept partial [coverage area] / return to Goal phase), constraints (updated if relaxed), goal_orientation, predictive_cue (re-entry: A8 if constraint relaxed; A4b if AS narrowed; A10 with partial-coverage allowed if accepted; cross-phase to G21 if Benefit scope change) | **Option sequencing (A.4):** Present resolution options in the following sequence: (1) relax a constraint — AI must propose a specific constraint relaxation and its impact on the goal; (2) narrow an AS — AI must propose which AS to narrow and why it still serves the Benefit; (3) accept partial coverage — AI must specify which AS remains uncovered and document it as a NOFIT compromise; (4) return to Goal phase — present only after stating explicitly why options (1)–(3) cannot preserve the goal. **NOFIT outcome in gate package:** Any A-NOFIT resolution is recorded as a flagged row in the AS-coverage matrix (source: "A-NOFIT compromise") and carried verbatim into A18 and A19. The G2 expert must explicitly see and acknowledge each compromise; A19 does not treat NOFIT resolutions as normal coverage. |
| A11 | Technology Review | [E] | Present the technology selection, rationale, and ruled-out alternatives; obtain expert confirmation that the choice is appropriate. | semantic_gist (selected technology + rationale, ruled-out options), retrieved_artifacts (cited evidence), relational_map (selected-technology→constraint), goal_orientation, uncertainty_signal, predictive_cue | episodic_trace (expert decision), semantic_gist (confirmed selection or /gm instruction), relational_map, goal_orientation, predictive_cue (PASS → A12; /gm → A11b) | Expert may confirm, add constraints, narrow scope, or reject. /ty proceeds to A12. /gm opens A11b. **Presentation structure:** Lead with Issue ("Is [selected technology] the right choice for this project's constraints and AS requirements?") and Conclusion (AI recommendation: CONFIRM or FLAG with one-sentence reason). Because the conclusion is non-self-evident without reviewing the evidence, present the technology rationale, ruled-out alternatives, and evidence citations as supporting material after the Conclusion. |
| A11b | Technology Revision ×N | [E] | Revise the technology selection per /gm feedback until the expert confirms or iteration limit is reached. | episodic_trace (prior exchange), semantic_gist (current selection + rationale), focal_entities, relational_map, goal_orientation, predictive_cue (termination condition), uncertainty_signal | episodic_trace (updated), semantic_gist (revised selection), relational_map, focal_entities, goal_orientation, uncertainty_signal | **Circuit-breaker:** Max N=5. Escalation as A4b: enumerate each unresolved finding individually; present explicit options (acceptance requires goal-tied rationale per finding); expert must choose one. |
| A12 | Technology Lock | [I] | Lock the technology choice; derive must/must_not constraints for Design; compress evidence using coverage-relevant closure; verify runner compatibility with locked Testing strategy. | semantic_gist (confirmed technology selection from A11/A11b, locked Testing strategy), retrieved_artifacts (all current), focal_entities, relational_map, goal_orientation, constraints, uncertainty_signal | semantic_gist (locked Technology), constraints (technology-derived: must_not(alternatives), must(chosen approach), follow(library/version if specified)), retrieved_artifacts (coverage-relevant: see A16 citation rule), relational_map (technology→testing-strategy compatibility edge, locked-technology→constraint edges), focal_entities (locked-technology entity finalized), goal_orientation, uncertainty_signal | **Runner compatibility check (early warning):** If the locked Technology is incompatible with the test runner confirmed at A6/A6b, route to A-REDIRECT(re-entry: A6, stale-components: [Testing strategy from A6/A6b, Technology]) rather than proceeding to A13. Re-entry at A6 allows the runner selection to be revised with knowledge of the actual Technology choice. This check is early warning only; A17 is the sole authority for AS coverage at G2. **Pending AS resolution:** For each AS flagged as pending at A6 (test method deferred pending Technology selection), confirm the test method is compatible with the locked Technology. If compatible, update relational_map (AS→test-method edge confirmed) and clear the pending flag in uncertainty_signal. If any pending AS remains incompatible with the locked Technology, route to A-REDIRECT(re-entry: A6, stale-components: [Testing strategy from A6/A6b, Technology]) rather than proceeding to A13. **Loop termination:** If a pending AS incompatibility routes back to A12 after a second A6 pass and the incompatibility is unresolved for the same AS, route to A-NOFIT for that AS rather than re-entering A6 again; A12 must not re-enter A6 more than once per pending AS. All pending AS must be individually resolved or escalated before A13 begins. |
| A13 | Design Scoping | [I] | Define what the Design must cover: satisfy locked Testing strategy (tests executable with locked Technology), use only locked Technology, address every AS, stay within Benefit scope. | semantic_gist (locked Testing strategy, locked Technology), constraints (A12 output), focal_entities (AS identifiers, locked-technology entity), relational_map (AS→test-method, technology→constraint), goal_orientation, uncertainty_signal | semantic_gist (design scope: required components list, excluded areas, scope constraint list), constraints (updated with design scope boundaries), relational_map (AS→required-component edges added), focal_entities (required design components added), goal_orientation, predictive_cue (design scope defined; proceed to A14), uncertainty_signal | If an AS cannot be addressed within locked Technology constraints, route to A-REDIRECT rather than proceeding to A14. |
| A14 | Design Draft | [I] | Draft the Design section of approach.md; each decision traced to a specific AS, test method, or Technology constraint it satisfies. | semantic_gist (design scope, locked Testing strategy, locked Technology), constraints, focal_entities (required design components), relational_map, goal_orientation, uncertainty_signal | semantic_gist (Design draft: structured decisions each with AS/test-method/tech-constraint trace), relational_map (design-decision→AS/test-method/tech-constraint edges added), focal_entities (concrete design entities: table names, service names, component names), goal_orientation, uncertainty_signal (decisions addressing no AS or test method flagged as scope violations), predictive_cue (Design Review ready) | **Pre-work verification criteria (define before drafting):** (1) every AS appears in at least one design decision trace; (2) no design decision is untraceable to an AS, test method, or Technology constraint; (3) all Technology constraints from A12 are addressed by at least one design decision. These are the criteria A15 evaluates against — defining them before drafting ensures A14's output is assessable, not open-ended. Each design decision must trace to at least one: (1) which AS it addresses, (2) which test method it enables, or (3) which Technology constraint it satisfies. A decision tracing to none is a scope violation, flagged in uncertainty_signal. |
| A15 | Design Review | [E] | Present the Design draft to the expert; obtain confirmation that the design is appropriate, complete, and correctly scoped. | semantic_gist (Design draft), constraints, relational_map, focal_entities, goal_orientation, uncertainty_signal, predictive_cue | episodic_trace (expert decision), semantic_gist (confirmed design or /gm instruction), relational_map, goal_orientation, predictive_cue (PASS → A16; /gm → A15b) | **Presentation structure:** Lead with Issue ("Does the Design draft address all Acceptance Scenarios within the locked Technology constraints?") and Conclusion (AI recommendation: CONFIRM or FLAG with one-sentence reason). Because the conclusion is non-self-evident without reviewing the design, present the AS→design-decision trace summary as Evidence after the Conclusion. |
| A15b | Design Revision ×N | [E] | Revise the Design draft per /gm feedback until the expert confirms or iteration limit is reached. | episodic_trace (prior exchange), semantic_gist (current Design draft), constraints, focal_entities, relational_map, goal_orientation, predictive_cue (termination condition), uncertainty_signal | episodic_trace (updated), semantic_gist (revised Design draft), relational_map, focal_entities, goal_orientation, constraints, uncertainty_signal | **Circuit-breaker:** Max N=5. Escalation as A4b: enumerate each unresolved finding individually; present explicit options (acceptance requires goal-tied rationale per finding); expert must choose one. |
| A16 | Design Lock | [I] | Lock the Design; compress evidence and relational_map to the coverage-relevant closure; merge Testing + Technology + Design constraints into a single final constraint set. | semantic_gist (confirmed Design draft from A15/A15b), retrieved_artifacts (all current), focal_entities, relational_map, goal_orientation, constraints, uncertainty_signal | semantic_gist (locked Design), retrieved_artifacts (pruned: coverage-relevant closure), relational_map (pruned: coverage-relevant closure), focal_entities (pruned: coverage-relevant closure), constraints (final: Testing + Technology + Design merged), goal_orientation, uncertainty_signal | **Coverage-relevant closure (pruning predicate):** Retain every artifact, edge, and entity on any path from an AS node to a test-method node, technology-constraint node, or design-decision node. This predicate ensures A17 (Consistency Validation) receives complete coverage state — a stricter "cited in design rationale only" would drop edges A17 needs. |
| A17 | Approach Consistency Validation | [I] | Verify the complete Testing→Technology→Design chain: (a) each AS covered by ≥1 test, (b) each test method executable with locked Technology, (c) Design implements all Technology requirements, (d) no AS orphaned. Produce AS-coverage matrix. | semantic_gist (locked Testing strategy, locked Technology, locked Design), constraints, focal_entities, relational_map, goal_orientation | semantic_gist (consistency report: pass/fail per criterion; AS-coverage matrix: each AS × {test method, technology support, design element, lock source}), goal_orientation, uncertainty_signal (broken links with specific stale-component list and re-entry step ID), predictive_cue (PASS → A18; any fail → A-REDIRECT) | **Control flow on failure:** A17 failure does not proceed to A18. A17 invokes A-REDIRECT with the re-entry step (computed from broken-link stale-component dependency closure) and stale-component list before any further step executes. A18 only starts on PASS. **Re-entry computation:** If broken link implicates a Design element → re-entry A13, Design lock invalidated. If broken link implicates a Technology constraint → re-entry A10, Design and Technology locks invalidated. If broken link implicates a test method → re-entry A6, all three locks invalidated. A17 is the sole authority for AS coverage at G2; A12's runner check is early warning only. **NOFIT rows:** AS-coverage matrix includes any A-NOFIT compromise rows (source: "A-NOFIT compromise"); these are flagged, not treated as normal coverage. |
| A18 | Gate Preparation | [I] | Package all three locked elements + AS-coverage matrix + constraint list + any NOFIT compromise rows into a structured gate package for expert review. | semantic_gist (locked Testing, locked Technology, locked Design, consistency report, AS-coverage matrix), retrieved_artifacts (coverage-relevant set from A16), constraints, relational_map, goal_orientation, uncertainty_signal | semantic_gist (gate package: structured summary of all locked outputs, AS-coverage matrix verbatim including NOFIT rows, constraint list, residual uncertainty items), retrieved_artifacts (final cited set), goal_orientation, predictive_cue (ready for A19) | Gate package must include verbatim: locked Testing strategy, locked Technology + rationale, locked Design, AS-coverage matrix (AS × {test method, technology support, design element, lock source}), NOFIT compromise rows (called out separately, not embedded), constraint list, evidence citations, residual uncertainty_signals. |
| A18a | approach.md Write | [I] | Assemble the gate package into approach.md and commit it to the branch, so that A19 presents the actual committed file rather than an in-memory summary. | semantic_gist (gate package from A18), retrieved_artifacts (final cited set), constraints, goal_orientation | (file artifact) approach.md committed to branch; episodic_trace (write result: path + commit SHA), predictive_cue (A19 ready) | Required sections: Testing strategy (locked), Technology selection + rationale (locked), Design (locked), AS-coverage matrix (verbatim), NOFIT compromise rows (separate section, not embedded), constraint list, evidence citations, residual uncertainty items. Verification: `approach.md` exists at expected path; all required sections present; `git log` confirms commit on branch. If write or commit fails, set uncertainty_signal (gap: approach.md write failed — [reason]) and halt. |
| A19 | Gate Presentation (G2) | [E] | Present the complete approach.md including AS-coverage matrix to the expert; obtain an explicit decision to proceed to the Delivery phase or redirect for revision. | semantic_gist (gate package from A18), retrieved_artifacts (final cited set), constraints, goal_orientation, uncertainty_signal, predictive_cue | episodic_trace (expert decision), semantic_gist (gate decision: PROCEED or REDIRECT with rationale), constraints (final approved set), goal_orientation, predictive_cue (PROCEED → Delivery phase; REDIRECT → re-entry step per A-REDIRECT) | Only step where inter-phase transition to Delivery is authorized. /ty proceeds to Delivery. /gm activates A-REDIRECT. NOFIT compromise rows require explicit expert acknowledgment before /ty is accepted. The expert reviews the committed approach.md produced by A18a. **Presentation structure:** Lead with Issue ("Does approach.md provide a grounded, executable plan for the Delivery phase?") and Conclusion (AI recommendation: PROCEED or REDIRECT with one-sentence reason). Then present two verification passes (always shown because the PROCEED/REDIRECT conclusion is non-self-evident without reviewing the phase artifacts): **(1) Goal alignment** — for each AS, confirm that the locked Testing strategy, Technology selection, and Design together constitute a plan that achieves the Benefit; no AS may be covered only by a NOFIT compromise row without explicit acknowledgment. **(2) Quality** — confirm that: (a) the Testing strategy follows established testing practices for the locked runner — meaning test files use the runner's documented naming convention and each test method has a verified automation path with the locked runner; FAIL if any test method is unexecutable with the locked runner or has no documented execution command; (b) the Technology rationale cites quantitative or comparative evidence (benchmark result, download metric, dated adoption survey) — FAIL if any Technology claim uses qualitative language alone (e.g., "popular," "widely used") without a traceable metric; (c) the Design is internally consistent — every design decision maps to ≥1 AS, test method, or Technology constraint in the AS-coverage matrix — FAIL if any decision has no trace. Binary predicate: PASS requires all three conditions to hold; FAIL if any fails. Expert must confirm each pass independently — by explicitly stating which pass is confirmed — before overall /ty is accepted; a /ty not referencing both passes is not accepted. The approved constraints in CCS Out become the seed for Delivery phase CCS_0. |
| A-REDIRECT | /gm Handler | [conditional] | Update CCS to reflect the redirect instruction; identify the correct re-entry step; invalidate all CCS components populated by steps at or after the re-entry point using the lock-step closure rule. | episodic_trace (/gm instruction or A17 failure signal), semantic_gist (current state at redirect), constraints, goal_orientation, uncertainty_signal | semantic_gist (redirect rationale, re-entry step ID), goal_orientation, predictive_cue (re-entry step ID), uncertainty_signal (updated with redirect reason and stale component list), constraints (updated if redirect changes constraints) | **Lock-step closure rule (invalidation):** A lock for element X is invalidated if and only if the re-entry point r ≤ the lock step for that element. Lock steps: Testing = A6, Technology = A12, Design = A16. Therefore: r ≤ A6: all three locks invalidated; A6 < r ≤ A12: Technology and Design locks invalidated (Testing preserved if no AS change); A12 < r ≤ A16: Design lock invalidated only; r > A16: no locks invalidated (re-running validation only). **A12 runner-contradiction re-entry:** re-entry = A6, stale-components = [Testing strategy from A6/A6b, Technology]; A6 and A6b re-execute, allowing the runner to be revised with knowledge of the actual Technology. **Cross-phase re-entry** (to Goal phase G21): entire Approach phase CCS invalidated; Approach phase suspended. **Expert-facing output:** When presenting a re-entry decision to the expert, translate the stale component list into plain language — e.g., "Because we are re-entering at A6, the Testing strategy, Technology selection, and Design decisions are all invalid and will be redone; the goals (AS and Benefit) remain unchanged." Do not use CCS component names (e.g., lock-step closure, coverage-relevant closure) in the expert-facing explanation. |
| A-NOFIT | No-Viable-Technology | [conditional] | (See A-NOFIT row above in step sequence.) | | | |

---

## Changes from v7

| Finding | Change |
|---------|--------|
| [B.2] A7 | Added "Pre-research population" note: complete source-type population must be stated before ecosystem research begins; unsearched source types must be reported. |
| [D.3] A2 | Restructured Presentation structure: separated Rationale from Conclusion; renamed "Evidence" to "Supporting material" (the investigation plan is not facts/numbers per D.3). |
| [A.4] A-NOFIT | Added option sequencing: options (1)–(3) are Technology-side resolutions; option (4) (return to Goal phase) may only be presented after AI states why (1)–(3) cannot preserve the goal. |
| [B.1] A6 | Strengthened "Runner confirmed": requires specific file path, key name, and value cited in retrieved_artifacts; locating the file without citing its content is not sufficient. |
| [C.5] A12 | Added loop-termination rule: if pending AS incompatibility recurs after a second A6 pass, route to A-NOFIT for that AS; A12 must not re-enter A6 more than once per pending AS. |

## Changes from v6

| Finding | Change |
|---------|--------|
| [B.1] A2 | Added "Provisional names not approved by /ty" note: A2 /ty approves investigation scope, not the provisional names; gate package must explicitly label names as unverified hypotheses; A5 must not treat their presence in the plan as evidence of their existence. |
| [B.2] A5 | Added "Pre-investigation scope" paragraph: before any file reads, the complete investigation population must be stated; any area in that pre-stated scope that was not read must be reported as uninspected. |
| [C.5] A4b | Circuit-breaker option (1) now requires goal-tied acceptance rationale per finding. |
| [C.5] A6c | "accept current" option now requires goal-tied rationale per finding. |
| [C.5] A11b, A15b | Circuit-breaker escalation now specifies "acceptance requires goal-tied rationale per finding." |
| [D.2] Conventions | Added AI-internal use note: CCS columns are AI-internal; experts see only Presentation structure notes. CCS component names not exposed in expert-facing output. |
| [D.2] A-REDIRECT | Added Expert-facing output note: re-entry decisions translated to plain language; CCS component names (lock-step closure, coverage-relevant closure) not used in expert-facing explanation. |

## Changes from v5

| Finding | Change |
|---------|--------|
| [C.4] A19 | Quality pass updated: "established testing practices" now defined as documented naming convention + verified automation path; Technology rationale now has binary FAIL criterion (no qualitative-only claims); Design consistency check requires explicit trace. Added binary PASS/FAIL predicates and requirement that expert confirms each pass independently. |
| [C.5] A12 | Added explicit pending AS resolution step: A12 must individually confirm or escalate each pending AS from A6 before proceeding to A13. |
| [B.1] A5 | Added provisional path verification: A5 must first confirm each provisional path from A1 plan exists before treating it as an investigation target; missing paths set uncertainty_signal immediately. |
| [D.3] A2 | Issue text now defines "grounded approach.md" inline; full investigation plan (AS→test-method sketch, provisional file/directory list, technology categories) now required as Evidence. |
| [D.3] A11 | Added Presentation structure: Issue + Conclusion first; technology rationale and evidence as supporting material. |
| [D.3] A15 | Added Presentation structure: Issue + Conclusion first; AS→design-decision trace as Evidence. |

## Changes from v4

| Finding | Change |
|---------|--------|
| [A.2] Phase completion state | Added per-condition verification method for each of the five completion conditions; added A18a as the explicit file-write step. |
| [A.4/C.5] A4 | Abort option now explicitly requires options (1) and (2) to be considered first; defined what "abort" means (phase suspended; goal.md requires revision before new /go). |
| [B.2] A5 | Uninspected areas now emit mandatory uncertainty_signals that block A6 finalization unless A6b expert explicitly accepts or requires additional inspection. |
| [B.2] A6 | Runner confirmation examples now labeled illustrative, not exhaustive; unfamiliar runners must check documentation before emitting uncertainty_signal. |
| [C.5] A4b | Circuit-breaker escalation now requires individual enumeration of each unresolved finding plus explicit options including abort-definition. |
| [C.5] A6c, A11b, A15b | Same circuit-breaker escalation pattern as A4b. |
| [D.2] CCS | Replaced "See acc.md for the full schema" with self-contained statement; acc.md is for implementation details only. |
| [D.3] A19 | Added explanation that both verification passes are always shown because PROCEED/REDIRECT is non-self-evident without reviewing the artifacts. |
| [A.2] A18a | New [I] step: assembles and commits approach.md before the gate presentation. |

## Changes from v3

| Finding | Change |
|---------|--------|
| [A.2] | Added Conventions section with phase completion state and locked-element summary table. |
| [B.1] A0 | Enumerated required goal.md sections: Situation, Pain, Benefit, AS list, out-of-scope items. |
| [B.1] A1 | All file/directory names and technology categories labeled as provisional hypotheses; A2 treats them as candidate scope, not verified facts. |
| [B.1/C.3] A5 | Added per-A3-question hypothesis + direct-observation verification criterion; added codebase scope statement with uninspected-area disclosure. |
| [B.1/C.3] A7 | Added per-technology hypothesis + verification method; adoption indicators now require citable source (URL + date). |
| [B.2/C.1] A3 | Added verification method for the AS→method mapping (three-step confirmation: A6 runner, A4 testability, A17 coverage). Added completeness requirement: all goal.md AS must appear. |
| [C.1] A2 | Added minimum adequacy criteria and minor/substantial revision definition. |
| [C.1] A14 | Added pre-work verification criteria that define the standard A15 evaluates against. |
| [C.4/D.3] A19 | Added 2-pass gate review structure (goal alignment + quality) and Issue+Conclusion-first presentation requirement. |
| [C.5] A4 | Added G21-returns-without-change deadlock resolution path. |
| [C.5] A6 | Added deferral rationale for Technology-dependent pending AS; defined "runner confirmed" as direct file observation. |
| [D.2] | Added Conventions section defining [I]/[E]/[R]/[conditional] type codes and /ty//gm commands. |

## Changes from v2

| Finding | Change |
|---------|--------|
| [M] PD-F10 | A6b now includes explicit mandate constraint: "A6b is strictly scoped to confirming that A5 codebase findings are compatible with the A4-approved AS→method mapping. Changes to test methods or AS require A-REDIRECT re-entry ≤ A3, not inline revision at A6b." |
| [M] PD-F11 | Added A6c [E] Testing Strategy Revision ×N (max N=5 with escalation). A6b now opens A6c on /gm rather than being silent on revision. |
| [M] PD-F12 | A-REDIRECT interval table rewritten to lock-step closure rule (see A-REDIRECT row). The rule now correctly specifies that any re-entry into the Technology branch (r ≤ A12) invalidates the Technology and Design locks, ensuring post-Testing-lock technology churn triggers re-confirmation at A12 (and A6 if runner changes). |
| [L] PD-F13 | A12 notes: "This check is early warning only; A17 is the sole authority for AS coverage at G2." A17 notes updated accordingly. |
| [L] PD-F14 | A-NOFIT: outcome recorded in AS-coverage matrix as NOFIT compromise row. A18: NOFIT rows appear verbatim and separately. A19: explicit expert acknowledgment of NOFIT rows required before /ty accepted. |
| [M] AD-F8 | A17 failure A-REDIRECT re-entry now uses dependency closure: re-entry computed from stale-component closure (broken link implicates Design → A13; Technology → A10; test method → A6), not the fixed interval table. Interval table in A-REDIRECT is replaced by the lock-step closure rule. |
| [M] AD-F9 | A12 runner-contradiction route changed from A-REDIRECT(re-entry: A5) to A-REDIRECT(re-entry: A6, stale-components: [Testing strategy, Technology]). Re-entry at A6 (not A5) allows the runner to actually be revised with knowledge of the actual Technology choice; A5 re-entry would repeat codebase investigation without unlocking the contradicting runner lock. |
| [L] AD-F10 | A9 type changed from [R] to [conditional]. Skip path defined: when A8 flags no uncertainty, A10 reads A8's CCS directly (no phantom Turn); A9 records "deep-dive not required" in uncertainty_signal on skip path. |
| [L] AD-F11 | A16 pruning predicate updated to coverage-relevant closure: "Retain every artifact, edge, and entity on any path from an AS node to a test-method, technology-constraint, or design-decision node." This ensures A17 receives complete coverage state; "cited in design rationale only" would be too strict. |
| Partial PD-F8 | Not further changed. The concern (bookkeeping steps bundled with judgment steps) is reduced by v2's removal of A5c; A12 and A16 bundle mechanical compression with judgment but this is inherent to Lock steps across the framework (same pattern as G12, G19 in Goal phase). Accepted as design pattern. |
