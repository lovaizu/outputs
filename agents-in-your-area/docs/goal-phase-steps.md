# Goal Phase — Step List v9

Produced by integrating two independent expert reviews (product discovery + AI agent design)
and a subsequent re-review pass, then refined against Action Principles assay findings (iterations 1–6).

---

## Conventions

**Step types**

| Code | Meaning |
|------|---------|
| [I] | AI-internal step — no user interaction; runs to completion and writes CCS |
| [E] | Expert gate — requires `/ty` or `/gm` before any next step executes |
| [R] | Research step — reads external sources and writes findings to CCS; no state changes to goal artifacts |
| [conditional] | Executes only when a specified trigger condition is met; skip path is explicitly defined |

**Commands:** `/ty` — approve and proceed. `/gm` — provide feedback or redirect; opens a revision loop or activates G-REDIRECT.

**CCS** (Compressed Cognitive State) — the bounded state passed between Turns. Nine components: `episodic_trace` (what just happened), `semantic_gist` (current task framing), `focal_entities` (entities being worked on), `relational_map` (how entities relate), `goal_orientation` (the end goal — Benefit + AS, unchanged once locked), `constraints` (what must not be done), `predictive_cue` (what to do next), `uncertainty_signal` (what is still uncertain), `retrieved_artifacts` (evidence with source citations). These definitions make this artifact self-contained; [acc.md](acc.md) documents implementation details (component encoding, session initialization). **AI-internal use:** CCS In/Out columns document AI state management only; experts see only the content described in each [E] step's Presentation structure notes. CCS component names are not exposed in expert-facing output.

**Phase completion state:** Goal phase is complete when: (1) `goal.md` is committed to the branch — verified by G26a: file exists at expected path with all required sections; (2) Situation, Pain, Benefit, and AS are all locked — verified by CCS lock flags (Situation: G6/G6b resolution; Pain: G12; Benefit: G19; AS: G24); (3) traceability chain (AS→Pain→Situation) has no broken links — verified by G25 traceability report showing zero orphans; (4) G27 `/ty` has been issued by the expert — verified by G27 CCS Out episodic_trace recording the /ty decision.

---

## Step List

| ID | Name | Type | Purpose | CCS In (key components) | CCS Out (key components, success path) | Notes |
|----|------|------|---------|------------------------|---------------------------------------|-------|
| G0 | Goal Intake | [I] | Initialize CCS from the /hi hearing transcript so that all downstream steps start from a verified common ground. | episodic_trace (raw /hi transcript) | episodic_trace (hearing summary), semantic_gist (initial framing), focal_entities (named actors and objects from hearing), goal_orientation (stated user goal), uncertainty_signal (gaps and ambiguities in hearing) | If /hi transcript is absent, set uncertainty_signal (gap: no hearing transcript) and halt with a request to provide input. |
| G1 | User Identification | [E] | Confirm which user segment is the target of the goal so that subsequent elicitation is scoped to a real, addressable population. | episodic_trace (G0 hearing summary), semantic_gist (initial framing), goal_orientation, uncertainty_signal | episodic_trace (confirmed segment), focal_entities (target segment, named stakeholders), goal_orientation (refined), uncertainty_signal (remaining unknowns) | |
| G2 | Elicitation Scoping — Situation | [I] | Define the exact questions to ask in G3 and the termination condition that will signal a satisfactory Situation statement. | episodic_trace (G1 output), focal_entities (target segment), goal_orientation, uncertainty_signal | semantic_gist (question set), goal_orientation, predictive_cue (termination condition: Situation passes factual specificity and scope tests) | Termination condition must be a checkable predicate, not a judgment. |
| G3 | Situation Draft | [E] | Elicit context from the expert and propose a first Situation statement scoped to the confirmed user segment. | episodic_trace (G1 output), semantic_gist (G2 question set), focal_entities, goal_orientation, predictive_cue (termination condition), uncertainty_signal | episodic_trace (expert responses), semantic_gist (Situation draft v1), focal_entities (updated), goal_orientation, uncertainty_signal (open factual gaps) | |
| G4 | Situation Revision ×N | [E] | Revise the Situation statement per /gm feedback until it meets the G2 termination condition or iteration limit is reached. | episodic_trace (prior exchange), semantic_gist (current Situation draft), focal_entities, goal_orientation, predictive_cue (termination condition), uncertainty_signal | episodic_trace (updated), semantic_gist (revised Situation draft), focal_entities, goal_orientation, uncertainty_signal | **Circuit-breaker:** Max N=5. On N=5 without /ty, fire an escalation Turn: (a) enumerate each specific unresolved finding from all N revision cycles — every finding that blocked /ty must be named individually, not subsumed into a general summary; (b) present explicit options: (1) accept current draft with each finding recorded as open uncertainty_signal (acceptance rationale must state why each open finding does not block the goal); (2) narrow scope; (3) abort phase — present this option only after stating explicitly why options (1) and (2) cannot serve the goal; the AI must name the specific goal-blocking reason for each before listing option (3). Delivery is blocked; re-entry requires a new /go with revised scope. Expert must choose one option before continuing. |
| G5a | Situation Evidence Research | [R] | Gather qualitative evidence (market data, user research, analyst reports) that corroborates or challenges the current Situation draft. | semantic_gist (Situation draft), focal_entities, goal_orientation, uncertainty_signal | retrieved_artifacts (qualitative evidence, each tagged with source and relevance), uncertainty_signal | **Pre-search population:** Before searching, enumerate all source types relevant to this claim type (e.g., academic databases, analyst reports, industry surveys, government statistics); unsearched types must be reported. **Parallel with G5b** (‖); results merged in G5c. **Hypothesis per claim (state before searching):** "Claim [C] is evidenced by [expected source type]." **Verification:** Each claim is confirmed (source cited with URL/reference) or confirmed absent — absence requires ≥2 independent source types, documented query terms, no result returned; "search executed" alone is not absence. Claims with neither status → uncertainty_signal (gap: no qualitative evidence for [C]). **Failure mode:** uncertainty_signal (gap: qualitative evidence unavailable — [reason]); G5c surfaces this to G6. |
| G5b | Situation Domain Quantitative Research | [R] | Gather quantitative data (size, frequency, cost, adoption metrics) scoped strictly to the Situation domain — not to any candidate Pain. | semantic_gist (Situation draft), focal_entities, goal_orientation, uncertainty_signal | retrieved_artifacts (quantitative data, each tagged with source, metric, and Situation relevance), uncertainty_signal | **Pre-search population:** Before searching, enumerate all source types relevant to this metric type (e.g., government statistics, industry reports, academic databases, market research firms); unsearched types must be reported. **Parallel with G5a** (‖). **Scope constraint:** Prohibited from pre-selecting or implying Pain candidates. **Hypothesis per metric (state before searching):** "Metric [M] is available from [expected source type]." **Verification:** Each metric is sourced (value + source + date cited) or confirmed unavailable — unavailability requires ≥2 independent source types, documented queries, no value returned. **Failure mode:** Same pattern as G5a. |
| G5c | Research Merge | [I] | Merge retrieved_artifacts from G5a and G5b into a single coherent set so that replacement semantics does not drop either branch's output. | retrieved_artifacts (G5a output), retrieved_artifacts (G5b output), uncertainty_signal (from each branch) | retrieved_artifacts (merged, deduplicated, branch tags preserved), uncertainty_signal (consolidated: gaps from either branch), semantic_gist (evidence summary) | Required because replacement semantics means one branch would overwrite the other without this merge Turn. G5c is the authoritative evidence source for G6 onward. |
| G6 | Situation Validation | [I] | Validate the Situation draft against merged evidence; flag any factual mismatch, unsupported claim, or critical evidentiary gap. | semantic_gist (Situation draft), retrieved_artifacts (G5c), goal_orientation, uncertainty_signal | semantic_gist (validation findings: supported / unsupported claims, flagged gaps), goal_orientation, uncertainty_signal (flag raised if mismatch), predictive_cue (PASS or BLOCK with reason) | G6 raises flags only; G6b presents them to the expert. |
| G6b | Situation Validation Review | [E] | Present G6's validation findings and obtain an explicit expert decision to proceed or revise. | semantic_gist (G6 validation findings), goal_orientation, uncertainty_signal (G6 flag), predictive_cue (PASS or BLOCK) | episodic_trace (expert decision), semantic_gist (updated with resolution), goal_orientation, predictive_cue (re-entry: G3 if BLOCK and expert chooses revision) | If G6 produced no flag (PASS), this step is a pass-through. If BLOCK, expert issues /gm to re-enter G3 or /ty to override with rationale. **Presentation structure:** Lead with Issue ("Do the Situation draft's key claims have adequate evidentiary support to proceed to Pain elicitation?") and Conclusion (PROCEED or BLOCK). If BLOCK (non-self-evident conclusion): add Rationale (one sentence of judgment reasoning — why the gap level is or is not material to proceeding — no claim listings here) and then list each flagged claim with its validation status as Supporting material. If PASS: a brief pass-through notice is sufficient; no Rationale or Supporting material required. |
| G7 | Elicitation Scoping — Pain | [I] | Define Pain elicitation questions and termination condition, informed by the validated Situation and merged evidence. | semantic_gist (validated Situation, G5c evidence summary), focal_entities, goal_orientation, uncertainty_signal | semantic_gist (Pain question set), goal_orientation, predictive_cue (termination condition: Pain is an observable user symptom, not a root cause or solution) | |
| G8 | Pain Candidates Draft | [E] | Elicit from the expert and propose multiple candidate Pain statements, each framed as an observable user symptom. | episodic_trace (G6b exchange), semantic_gist (G7 question set, validated Situation), focal_entities, goal_orientation, predictive_cue (G7 termination condition), uncertainty_signal | episodic_trace (expert responses), semantic_gist (candidate Pain list), focal_entities (updated), goal_orientation, uncertainty_signal | |
| G9 | Pain Revision ×N | [E] | Revise the candidate Pain set per /gm feedback until it meets G7's termination condition or iteration limit is reached. | episodic_trace (prior exchange), semantic_gist (current candidate Pain set), focal_entities, goal_orientation, predictive_cue (G7 termination condition), uncertainty_signal | episodic_trace (updated), semantic_gist (revised candidate Pain set), focal_entities, goal_orientation, uncertainty_signal | **Circuit-breaker:** Max N=5. Escalation as G4: enumerate each unresolved finding individually; if no direction received, present options (1) accept with open uncertainty_signal (acceptance rationale must state why each open finding does not block the goal) / (2) narrow scope / (3) abort; expert must choose one. |
| G10 | Pain Prioritization and Selection | [E] | Force selection of exactly one primary Pain from the candidate set, with explicit expert rationale for the choice. | episodic_trace (G9 output), semantic_gist (revised candidate Pain set), focal_entities, goal_orientation, retrieved_artifacts (G5c evidence), uncertainty_signal | episodic_trace (expert selection and rationale), semantic_gist (selected Pain with rationale), focal_entities (updated), goal_orientation, uncertainty_signal | Selection must name one Pain; partial selections are not accepted. Expert issues /ty to confirm or /gm to reopen G9. |
| G11 | Pain Evidence Collection | [R] | Gather quantitative data, user feedback, and analogues specific to the selected Pain, incorporating prior Situation artifacts. | semantic_gist (selected Pain, validated Situation), retrieved_artifacts (G5c as prior context), focal_entities, goal_orientation, uncertainty_signal | retrieved_artifacts (Pain-specific evidence, incorporating prior Situation artifacts; each tagged with source and Pain relevance), uncertainty_signal | This Turn writes a fresh CCS including both prior Situation artifacts and new Pain evidence. **Hypothesis per evidence type (state before searching):** "Pain [P] is evidenced as observable symptom by [expected source type]." **Verification:** Each evidence type is sourced (value + source cited) or confirmed unavailable; no evidence in any category → uncertainty_signal (gap: no Pain evidence — [reason]). G12 decides whether to block. |
| G12 | Pain Lock | [I] | Confirm that the selected Pain is an observable user symptom only — not a root cause, not a solution — and that evidence supports its significance. | semantic_gist (selected Pain, G10 rationale), retrieved_artifacts (G11 Pain evidence), focal_entities, goal_orientation, uncertainty_signal | semantic_gist (locked Pain statement), goal_orientation, uncertainty_signal (updated), predictive_cue (LOCKED or BLOCK with reason) | If Pain contains causal language or solution framing, set BLOCK and route back to G10 via predictive_cue. Pain Lock is a prerequisite for Root Cause Analysis (G13). |
| G13 | Root Cause Analysis | [I] | Trace the root cause of the locked Pain to a depth of ≤ 3 causal hops, producing a cause chain that Benefit elicitation can invert. | semantic_gist (locked Pain from G12), retrieved_artifacts (G11 Pain evidence), focal_entities, goal_orientation, uncertainty_signal | semantic_gist (root cause chain: Pain → cause 1 → … → root, max 3 hops), focal_entities (updated with causal actors), goal_orientation, relational_map (causal structure), uncertainty_signal | Root cause must analyze the confirmed, locked Pain — not the candidate set. Positioned after G12 for this reason. If causal chain cannot be established within 3 hops, set uncertainty_signal (gap: causal chain unclear). **Causal hop citation:** Each causal hop must cite at least one retrieved artifact (from G5c or G11) as supporting evidence. A hop with no supporting artifact is set as uncertainty_signal (gap: causal hop [N] — no supporting evidence) rather than asserted as fact. |
| G14 | Similar Cases Research — Pain Axis | [R] | Gather analogous cases and precedents where the same root cause was addressed, to inform Benefit scoping. | semantic_gist (locked Pain, root cause chain from G13), relational_map (causal structure), focal_entities, goal_orientation, uncertainty_signal | retrieved_artifacts (analogous cases, each tagged with root cause match and Benefit outcome), uncertainty_signal | Precedes Elicitation Scoping — Benefit (G15) so that G15 is grounded in real analogues. **Hypothesis per case type (state before searching):** "Analogous case [type] exists for root cause [RC] with outcome [expected metric]." **Verification:** Each case type is found (source + root cause match + outcome metric cited) or confirmed absent — absence requires ≥2 independent source types, queries from G13 root cause, no result returned; "search executed" alone is not absence. **Failure mode:** uncertainty_signal (gap: no analogous cases found — [reason]); G15 adjusts confidence accordingly. |
| G15 | Elicitation Scoping — Benefit | [I] | Define Benefit elicitation questions and termination condition, informed by the root cause chain and G14 analogous cases. | semantic_gist (locked Pain, root cause chain), retrieved_artifacts (G14 analogous cases), focal_entities, goal_orientation, uncertainty_signal (G14 gaps) | semantic_gist (Benefit question set), goal_orientation, predictive_cue (termination condition: Benefit traces to root cause, is not the inverse of Pain, is strategically addressable) | Follows G14 so the question set is grounded in research, not vacuum reasoning. |
| G16 | Benefit Draft | [E] | Propose a Benefit statement derived from the root cause chain; present alongside the inverse-of-Pain test result for expert review. | episodic_trace (G12 Pain Lock, G13 root cause chain), semantic_gist (G15 question set, G14 analogues), focal_entities, goal_orientation, predictive_cue (G15 termination condition), uncertainty_signal | episodic_trace (expert responses), semantic_gist (Benefit draft v1, with inverse-of-Pain test result noted), focal_entities (updated), goal_orientation, uncertainty_signal | Inverse-of-Pain test is presented for transparency; structural validation is deferred to G17b. |
| G17 | Benefit Revision ×N | [E] | Revise the Benefit draft per /gm feedback until it meets G15's termination condition or iteration limit is reached. | episodic_trace (prior exchange), semantic_gist (current Benefit draft), focal_entities, goal_orientation, predictive_cue (G15 termination condition), uncertainty_signal | episodic_trace (updated), semantic_gist (revised Benefit draft), focal_entities, goal_orientation, uncertainty_signal | **Circuit-breaker:** Max N=5. Escalation as G4: enumerate each unresolved finding individually; if no direction received, present options (1) accept with open uncertainty_signal (acceptance rationale must state why each open finding does not block the goal) / (2) narrow scope / (3) abort; expert must choose one. |
| G17b | Benefit Quality Validation | [I] | Independently verify that the Benefit draft (a) is not the inverse of Pain and (b) traces structurally to the root cause from G13, not merely to the Pain surface. | semantic_gist (revised Benefit draft from G17, root cause chain from G13, locked Pain from G12), focal_entities, goal_orientation | semantic_gist (validation findings: root-cause trace result, inverse-of-Pain test result), goal_orientation, uncertainty_signal (PASS or BLOCK with specific structural failure), predictive_cue (PASS → G18; BLOCK → re-enter G17 with failure detail) | Independent structural check — does not rely on G16's self-applied test. If BLOCK, predictive_cue routes back to G17 with the specific failure. G18 only receives PASS cases. |
| G18 | Benefit Strategic Relevance Check | [E] | Confirm with the expert that the validated Benefit is a current strategic priority worth pursuing in the Approach phase. | episodic_trace (G17 exchange), semantic_gist (Benefit draft, G17b validation findings), focal_entities, goal_orientation, uncertainty_signal | episodic_trace (expert confirmation or redirect), semantic_gist (Benefit with strategic relevance confirmed), goal_orientation, uncertainty_signal | A /gm here may signal a need to revisit root cause or Pain selection; G-REDIRECT handles routing. |
| G19 | Benefit Lock + Evidence Compression | [I] | Lock the Benefit statement and drop all artifacts not cited in any Lock step output or locked semantic_gist component. | semantic_gist (confirmed Benefit, all prior Lock step outputs: G6, G12), retrieved_artifacts (full evidence set), focal_entities, goal_orientation | semantic_gist (locked Benefit), retrieved_artifacts (cited artifacts only), focal_entities (pruned), goal_orientation, uncertainty_signal | **Citation definition:** An artifact is "cited" if referenced in the CCS Out of any Lock step (G6, G12, G19) or in any locked semantic_gist component. All uncited artifacts are dropped. |
| G20 | Consistency Validation | [I] | Verify that Situation, Pain, and Benefit form a logically consistent chain; produce a re-entry routing instruction if any inconsistency is found. | semantic_gist (locked Situation, locked Pain, locked Benefit), relational_map (G13 causal structure), focal_entities, goal_orientation | semantic_gist (consistency check findings), goal_orientation, uncertainty_signal (inconsistency detail if found), predictive_cue (re-entry step ID: Situation–Pain mismatch → G3; Pain–Benefit mismatch → G15; multi-element → earliest inconsistent element) | Re-entry routing is required: G20 must produce predictive_cue (re-entry step ID) whenever an inconsistency is found. The flag alone is insufficient. |
| G21 | AS Draft + Out-of-Scope | [E] | Draft Acceptance Scenarios in the required format and define what is explicitly out of scope. | semantic_gist (locked Situation, Pain, Benefit, root cause chain), focal_entities, goal_orientation, relational_map, uncertainty_signal | episodic_trace (expert input), semantic_gist (AS draft set, out-of-scope list), focal_entities, goal_orientation, uncertainty_signal | **AS format:** Each AS states an observable, user-visible state; no solution mechanism; no internal system state. Example: "User can [observable action] without [pain symptom]." G23's format-compliance check references this definition. |
| G22 | AS Revision ×N | [E] | Revise the AS set per /gm feedback until testability requirements are met or iteration limit is reached; on re-entry from G24, consume G23's uncertainty_signal to target specific failures. | episodic_trace (prior exchange), semantic_gist (current AS set, out-of-scope list), focal_entities, goal_orientation, predictive_cue (testability termination condition), uncertainty_signal (G23 per-AS failure detail if re-entering from G24) | episodic_trace (updated), semantic_gist (revised AS set), focal_entities, goal_orientation, uncertainty_signal | **Circuit-breaker:** Max N=5. Escalation as G4: enumerate each unresolved finding individually; if no direction received, present options (1) accept with open uncertainty_signal (acceptance rationale must state why each open finding does not block the goal) / (2) narrow scope / (3) abort; expert must choose one. On re-entry from G24, CCS In must include G23's uncertainty_signal so revisions target the specific flagged items. |
| G23 | AS Testability Check | [I] | Verify each AS against three criteria: (1) falsifiable, (2) Pain-traceable — resolves the Pain, not merely topically related, (3) format-compliant per G21's AS format definition. | semantic_gist (AS set, G21 AS format definition, locked Pain from G12), relational_map, focal_entities, goal_orientation | semantic_gist (per-AS check results: PASS or FAIL with criterion), uncertainty_signal (per-AS failure detail for any FAIL), focal_entities (AS set with flagged items), goal_orientation, predictive_cue (overall: PASS → G24 success path; any FAIL → G24 failure path) | Criterion 2 requires that if all AS are met, the Pain is no longer observable — "topically related" is insufficient. |
| G24 | AS Lock | [I] | Lock the AS set if G23 passes; if G23 flagged failures, emit failure-path CCS that routes back to G22 for targeted revision. | semantic_gist (G23 per-AS check results), uncertainty_signal (G23 per-AS failure detail), focal_entities (flagged AS set), goal_orientation, predictive_cue (G23 overall: PASS or FAIL) | **Success path:** semantic_gist (locked AS set), goal_orientation, predictive_cue (proceed to G25). **Failure path:** uncertainty_signal (per-AS failure detail from G23), focal_entities (AS set with flagged items), predictive_cue (re-entry: G22). | Failure-path CCS Out is distinct from success-path. G22 re-entry must consume the failure-path uncertainty_signal. |
| G25 | Traceability Verification | [I] | Verify the complete AS→Pain→Situation chain: each AS traces to locked Pain, Pain traces to validated Situation, no orphaned elements. | semantic_gist (locked AS set, locked Pain, locked Situation), relational_map (G13 causal structure), focal_entities, goal_orientation | semantic_gist (traceability report: chain status per AS), goal_orientation, uncertainty_signal (broken links if any), predictive_cue (PASS → G26; broken link → re-entry at earliest broken element) | |
| G26 | Gate Preparation | [I] | Package all locked artifacts, the traceability report, and the evidence set into a structured gate package for expert review in G27. | semantic_gist (locked Situation, Pain, Benefit, AS set, traceability report), retrieved_artifacts (cited set from G19), relational_map, focal_entities, goal_orientation, uncertainty_signal | semantic_gist (gate package: structured summary of all locked outputs), retrieved_artifacts (final cited set), goal_orientation, predictive_cue (ready for G27) | Gate package must include: locked Situation, Pain, Benefit, AS set, traceability report, evidence citations, and any residual uncertainty_signals. |
| G26a | goal.md Write | [I] | Assemble the locked artifacts into goal.md and commit it to the branch, so that G27 presents the actual committed file rather than an in-memory summary. | semantic_gist (gate package from G26), retrieved_artifacts (final cited set), goal_orientation | (file artifact) goal.md committed to branch; episodic_trace (write result: path + commit SHA), predictive_cue (G27 ready) | Required sections: Situation (locked), Pain (locked), Benefit (locked), Acceptance Scenarios (locked AS set + out-of-scope), Traceability summary (G25 report), Evidence citations (G19 cited set). Verification: `goal.md` exists at expected path; all required sections present; `git log` confirms commit on branch. If write or commit fails, set uncertainty_signal (gap: goal.md write failed — [reason]) and halt. |
| G27 | Gate Presentation | [E] | Present the gate package to the expert and obtain an explicit decision to proceed to the Approach phase or redirect for revision. | semantic_gist (gate package from G26), retrieved_artifacts (final cited set), focal_entities, goal_orientation, uncertainty_signal (residual), predictive_cue (G26 readiness signal) | episodic_trace (expert decision), semantic_gist (gate decision: PROCEED or REDIRECT with rationale), goal_orientation, predictive_cue (PROCEED → Approach phase; REDIRECT → re-entry step per G-REDIRECT) | Only step where inter-phase transition is authorized. /ty proceeds to Approach; /gm activates G-REDIRECT with gate package as context. The expert reviews the committed goal.md produced by G26a. **Presentation structure:** Lead with Issue ("Do the locked Situation, Pain, Benefit, and AS together define a goal we should pursue?") and Conclusion (AI recommendation: PROCEED or REDIRECT with one-sentence reason). Then present the two verification passes (the passes are always shown here because the conclusion — PROCEED or REDIRECT — is non-self-evident without reviewing the phase artifacts): **(1) Goal alignment** — for each AS, confirm that resolving the Pain as defined by the Benefit is what the AS verifies; no AS should test a mechanism or solution. **(2) Quality** — confirm that: (a) every evidence citation names a real, dated source (publication name, URL, or report title with retrieval year) — FAIL if any citation is anonymous, undated, or stated without a traceable reference; (b) the root cause chain from G13 has ≤3 causal hops expressed in observable terms AND each hop cites at least one retrieved artifact per G13's citation requirement — FAIL if the chain exceeds 3 hops, uses mechanistic framing, or any hop has no cited retrieved artifact; (c) each locked AS follows G21's format (observable user-visible state; no solution mechanism; no internal system state) — FAIL if any AS describes an implementation detail or mechanism. Binary predicate: PASS requires all three conditions to hold; FAIL if any fails. Expert must confirm each pass independently — by explicitly stating which pass is confirmed — before overall /ty is accepted; a /ty not referencing both passes is not accepted. |
| G-REDIRECT | /gm Handler | [conditional] | Update CCS to reflect the redirect instruction and identify the correct re-entry step, invalidating all CCS components whose source steps fall at or after that re-entry point. | episodic_trace (/gm instruction), semantic_gist (current state at redirect), focal_entities, goal_orientation, uncertainty_signal | semantic_gist (redirect rationale, re-entry step ID), goal_orientation, predictive_cue (re-entry step ID), uncertainty_signal (updated with redirect reason and stale component list) | **CCS invalidation rule:** On re-entry to step X, all components populated by steps ≥ X are stale; enumerate them explicitly in CCS Out. **Fork-join rule:** If re-entry target ≤ G5a, re-run both G5a and G5b and G5c. **Expert-facing output:** When presenting a re-entry decision to the expert, translate the stale component list into plain language — e.g., "Because we are returning to G3, the Situation draft and all subsequent work (Pain, Benefit, AS) is invalid and will be redone; the goals remain unchanged." Do not use CCS component names in the expert-facing explanation. |

---

## Changes from v8

| Change | Description |
|--------|-------------|
| Slim pass | Removed changelogs v2–v6; compressed G5a, G5b, G11, G14 Notes (repeated research pattern). No logic changes. |

## Changes from v7

| Finding | Change |
|---------|--------|
| [B.2] G5a, G5b | Added "Pre-search population" note: complete source-type population must be stated before any search begins; unsearched source types must be reported. |
| [D.3] G6b | Restructured Presentation structure: separated Rationale from Conclusion; made Supporting material section conditional on BLOCK; PASS path requires only a pass-through notice. |
| [A.4] G4 (propagates to G9, G17, G22 via "Escalation as G4") | Abort option (3) now requires AI to state explicitly why options (1) and (2) cannot serve the goal before listing option (3). |
| [C.4] G27 | Quality criterion (b) extended: FAIL if any causal hop in the G13 chain has no cited retrieved artifact, per G13's citation requirement. |

